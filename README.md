# do-freebsd-hardenedbsd

## Installation

Since FreeBSD does not ship with git we install it and run the install script.
```bash
pkg install git
git clone https://github.com/antonva/do-freebsd-hardenedbsd
cd do-freebsd-hardenedbsd
chmod +x install.sh
./install.sh
```
